<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
        <img id="logo" src="{{ asset('/assets/img/logo.png')}}" alt="Logo">
        <a class="navbar-brand ml-md-5" href="{{ path("homepage") }}"><h1>Green Leaf Market</h1></a>
        <ul id="navbar" class="navbar-nav mr-auto ml-5 mt-2 mt-lg-0">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('homepage') }}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Offres d'emplois</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Points de vente</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Contact</a>
            </li>
        </ul>
    </div>
    <div>
        {% if app.user %}
            <a class="btn btn-primary mr-4" href="{{ path('profile') }}">Mon profil</a>
            <a class="btn btn-danger" href="{{ path('app_logout') }}">Se déconnecter</a>
        {% else %}
            <a class="btn btn-outline-primary mr-4" href="{{ path('app_register') }}">S'inscrire</a>
            <a class="btn btn-primary" href="{{ path('app_login') }}">Se connecter</a>
        {% endif %}
    </div>
</nav>