{% extends 'base.html.twig' %}

{% block title %}Modification d'un article{% endblock %}

{% block body %}
    <div class="container-md mt-5">
        <h2>Modification de l'article : {{ meeting.title|capitalize }}</h2>
        <a class="text-primary" href="{{ path('meeting') }}">Retourner aux articles</a>
        <div class="mt-5">
            {{ form(meetingForm) }}
        </div>
    </div>
{% endblock %}